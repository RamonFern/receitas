import { StyleSheet, Text, View } from 'react-native';

export function Datail() {
  return (
    <View style={styles.container}>
      <Text>Página datelhes da receita...</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'blue'
  },
});